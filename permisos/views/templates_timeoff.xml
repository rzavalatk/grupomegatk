<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--<template id="hr_holidays.TimeOffCard_inh" inherit_id="hr_holidays.TimeOffCard">
        <xpath expr="//div[@class='text-uppercase']" position="replace">
            <div class="text-uppercase">
                 Mostrar siempre Days, Hours y Minutes -->
                <!--<span>days, hours, minutes</span>-->

                <!-- Puedes mostrar los valores directamente si los tienes 
                 <t t-esc="data.dias_empleado"/> días, <t t-esc="data.horas_empleado"/> horas, <t t-esc="data.minutos_empleado"/> minutos 

                <t t-if="props.requires_allocation" name="duration_type">
                    available
                </t>
                <t t-else="">
                    taken
                </t>

                <t t-if="show_popover">
                    <TimeOffCardPopover
                        allocated="data.max_leaves"
                        approved="data.leaves_approved"
                        planned="data.leaves_requested"
                        left="data.virtual_remaining_leaves"
                        usable="data.usable_remaining_leaves" />
                </t>
            </div>
        </xpath>
    </template>-->
</odoo>
